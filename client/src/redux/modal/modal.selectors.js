export const getIsOpen = (state) => !!state.modal.mode;

export const getMode = (state) => state.modal.mode;

export const getData = (state) => state.modal.data;

export const getIsPickingDate = (state) => state.modal.isPickingDate;
